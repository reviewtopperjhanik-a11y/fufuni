<!--
  AUTO-GENERATED by apps/mcp/src/lib/generate-knowledge-modules/generate.ts
  Do not edit manually. Run the script to regenerate.
  To add or modify topics, edit files in apps/mcp/src/knowledge/topics/
  description:  Call this when an overview of the Fufuni framework architecture and conventions is needed.
  model:        mistral/magistral-medium-latest
  tokens_in:    29074
  tokens_out:   3074
  api_endpoint: https://gateway.ai.cloudflare.com/v1/___cloudflare_account_id___/default/compat
  manual_facts_checksum: 041690e96af3089f72ba0af15a25dc9cc96a498d0294f24e7fcea9cc98806567
  sources_checksum: 4a727425f117a9031cee3f1361aece28ddf2a644767758db04cf05751069165d
  source_file_hashes:
    README.md: 5990ecab00f4a3c3cbccca147e754cd3bfeb4938f792235d7111fd76719b00c2
    apps/merchant/src/index.ts: e4c87e9fc4cf406dc9389668af3e599dded64048717c4642c1969f18bae3d7b9
-->
<!--mcp-tags: ["backend","frontend","general","commerce","design"]-->

## Framework Overview

Fufuni is an open-source, AI-native e-commerce framework designed to run entirely within free tiers of Cloudflare Workers, Auth0, Stripe, and Mailgun. It provides a complete solution for online stores with features including product catalog management, shopping cart, checkout, order processing, and customer management.

## Stack

| Layer | Technology | Notes |
|-------|------------|-------|
| Frontend | React 19, Vite, HeroUI v3 | SPA with admin panel |
| Backend | Cloudflare Workers, Hono, Zod-OpenAPI | Edge functions with OpenAPI docs |
| Database | Durable Objects with SQLite | Embedded, no connection pools |
| Storage | Cloudflare R2 | Product images and assets |
| Auth | Auth0 | Passwordless and social login |
| Payments | Stripe | Checkout and webhooks |
| Email | Mailgun | Transactional emails |
| Deployment | GitHub Pages (frontend), Cloudflare Workers (backend) | Fully automated CI/CD |

## Monorepo Structure

The Fufuni monorepo uses Turborepo and consists of three main workspaces:

- `apps/client`: React frontend (storefront + admin)
- `apps/merchant`: Cloudflare Worker backend (Hono + Zod-OpenAPI)
- `apps/mcp`: MCP server for AI knowledge base

Key directories:
- `apps/merchant/migrations`: SQL migration files
- `apps/merchant/src/do.ts`: Durable Object with SQLite schema
- `apps/client/src/config/site.ts`: Frontend navigation configuration

## Request Lifecycle

1. Client request → Cloudflare Worker (Hono app)
2. Authentication via API key (`pk_`/`sk_`) or Auth0 JWT
3. Worker → Durable Object stub (single SQLite instance)
4. Durable Object executes request against embedded SQLite
5. Response flows back: DO → Worker → Client

```typescript
// Example request handler in apps/merchant/src/index.ts
app.get('/v1/products/:id', async (c) => {
  const db = c.get('db');
  const product = await db.getProduct(c.req.param('id'));
  return c.json(product);
});
```

## Key Conventions

- **API Keys**: `pk_` prefix for public, `sk_` prefix for admin/secret (legacy, prefer Auth0 JWT)
- **Authentication**: Auth0-only with RBAC via JWT permissions (e.g., `admin:store`, `ai:api`)
- **Database Schema**: Changes require updates in 3 places:
  - `SCHEMA` constant in `do.ts`
  - `ensureInitialized()` function in `do.ts`
  - New numbered SQL file in `apps/merchant/migrations/`
- **Navigation**: Configured in `siteConfig()` with permission-based visibility
- **Error Handling**: Global error handler returns structured responses
- **Free Tier**: Designed for 100% free operation using Cloudflare Workers (100k req/day), R2 (10GB), KV (100k reads), Auth0 (7.5k MAU), GitHub Pages, and Mailgun (3k emails/month)
- **Deployment**: Three GitHub Actions workflows for backend, frontend, and demo reset
- **UCP Endpoints**: Exposed at `/.well-known/ucp` and `/ucp/v1/*` for AI shopping agents

```json
// Example UCP endpoint response
{
  "capabilities": [
    "dev.ucp.shopping.checkout",
    "dev.ucp.shopping.browse",
    "dev.ucp.shopping.catalog"
  ]
}
```