{
  "name": "fufuni-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run gen-knowledge && (trap 'kill 0' EXIT; npx wrangler dev --port 8788 --inspector-port 9230 & sleep 2 && npx @modelcontextprotocol/inspector http://localhost:8788/mcp; wait)",
    "dev:env": "npm run gen-knowledge && (trap 'kill 0' EXIT; npx wrangler dev --port 8788 --inspector-port 9230 & sleep 2 && npx @modelcontextprotocol/inspector http://localhost:8788/mcp; wait)",
    "build": "npm run gen-knowledge && wrangler deploy --dry-run",
    "deploy": "npm run gen-knowledge && wrangler deploy",
    "gen-knowledge": "tsx scripts/gen-knowledge.ts",
    "stdio": "npm run gen-knowledge && tsx src/stdio.ts",
    "typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.11.4",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.84.0"
  }
}
